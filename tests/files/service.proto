syntax = "proto3";

package service;

message Request {
    string value = 1;
}

message Response {
    string value = 1;
}

service MyService {
    rpc Foo (Request) returns (Response);
    rpc Bar (Request) returns (Response);
}
