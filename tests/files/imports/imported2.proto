syntax = "proto3";

package imported2.foo.bar;

import "imported_duplicated_package.proto";

enum Imported2Enum {
    A = 0;
    B = 1;
}

message Imported2Message {
    ImportedDuplicatedPackageMessage v1 = 1;
}
